Signal-Slot library for Subscription Pattern (known methods instead of binding)

Library requires C++ compiler supporting C++11 features.

Library has very minimal dependecies:
 - std::forward_list
 - std::set
 - std::mutex
